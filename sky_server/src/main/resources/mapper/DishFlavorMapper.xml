<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.mapper.DishFlavorMapper">

    <!-- 批量插入菜品口味数据 -->
    <insert id="insertBatch">
        insert into dish_flavor (dish_id, name, value) values
        <foreach collection="list" item="dishFlavor" separator=",">
            (#{dishFlavor.dishId}, #{dishFlavor.name}, #{dishFlavor.value})
        </foreach>
    </insert>

    <delete id="deleteBatch">
        delete from dish_flavor where dish_id in
        <foreach collection="dishIds" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

</mapper>